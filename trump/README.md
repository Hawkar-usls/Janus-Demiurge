# TRUMP — candidate runtime tissue

TRUMP is allowed to wake inside JANUS before scientific release.

Current state:

```text
CANDIDATE_RUNTIME_TISSUE
wake/use/experiment = allowed
proof/scientific-promotion authority = false
P_VS_NP = OPEN
```

The runtime does not copy Fundamentum research code into Demiurge. Instead, `TRUMP_MANIFEST.json` pins an exact Fundamentum commit/path/blob. `trump_candidate.py` fetches that exact object, recomputes its Git blob identity, verifies required entrypoints, and only then imports it as candidate tissue.

Canonical first candidate:

```text
Hawkar-usls/Janus-Fundamentum
@377668585178995217b681d5ddb1b34b0f8dc386
experiments/direct/janus_unified_proof_carrying_akinator_jec.py
```

The candidate itself is explicitly fail-closed and may return `OPEN`. A finite run, green CI, candidate improvement, or successful self-test cannot promote `P=NP` or any other open scientific claim.

## Current research frontier — pointer only

Runtime authority and research frontier are intentionally separate.

The current candidate research frontier is recorded in:

```text
trump/TRUMP_RESEARCH_FRONTIER_POINTER.json
```

At the 2026-09-03 R44X frontier, Fundamentum has an exact finite SAT/UNSAT collision for connected bipolar 3-CNF formulas with identical unary polarity degrees and identical complete signed literal-pair co-occurrence multiplicities. This refutes order<=2 local marginal signatures as a sufficient exact universal decision key, while leaving global/compositional polynomial representations open.

The frozen theorem target is now:

```text
TRUMP_UNIVERSAL_PROOF_CARRYING_DECOMPOSITION_LEMMA
```

That target must discharge universal exit, exact semantics, polynomial discovery/local work, polynomial state envelope, polynomial termination, and end-to-end replay before any `P=NP` promotion is possible.

This pointer does **not** replace the primary executable candidate and does not grant theorem authority:

```text
RESEARCH_FRONTIER_POINTER != RUNTIME_PROMOTION != THEOREM_AUTHORITY
```

## JANUS Soul — hindsight obligation compiler

`janus_soul_hindsight.py` is the first executable candidate for the JANUS Soul layer.

Its design translates retrospective learning into proof-safe mechanics:

```text
verified past failure
  -> exact failure receipt
  -> bounded known-bad guard
  -> future rejection of the same certified dead route
```

The v1 runtime deliberately refuses the dangerous leap:

```text
PAST_IS_OBSERVABLE != FUTURE_IS_KNOWN
HINDSIGHT != ORACLE
NOT_KNOWN_BAD != PROVED_GOOD
```

JANUS therefore has two faces:

- **LEARNING_FACE** may inspect append-only history and compile candidate lessons.
- **THEOREM_FACE** freezes one finite snapshot before the input is seen and forbids runtime learning or post-hoc repair.

The Soul also carries the `NO_HOTEL_CALIFORNIA` progress gate: a state transition is not progress merely because the state changed. A theorem-mode macrostep must strictly decrease a frozen, polynomially capped well-founded rank; otherwise it is rejected.

The key remaining mathematical bridge is still open:

```text
HINDSIGHT_COVERAGE_AND_REMAINDER_LEMMA
```

Historical guards can prove that some routes are bad. They do **not** yet prove that every reachable nonterminal SAT state always leaves at least one exact useful action that can be found within polynomial total work.

Self-improvement is allowed as candidate work: JANUS may generate/test/compare improved TRUMP candidates and stage reviewed candidate-branch changes. Direct `main` writeback and automatic merge remain disabled.

Canonical rendezvous remains:

```text
Hawkar-usls/Janus-Demiurge/trump/TRUMP_MANIFEST.json
```

A future proof-authorized release may upgrade the manifest in place only after exact Fundamentum proof/replay release binding and adversarial gates.
